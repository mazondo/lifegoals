= simple_form_for([@goal, @todo], :html => {:class => "form-horizontal"}) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :complete

  .form-actions
    = f.button :submit, :class => "btn btn-primary"
    = link_to "cancel", @goal, :class => "btn"
